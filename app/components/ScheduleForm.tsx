"use client";
import { FormEvent, useState } from "react";
import ArrowIcon from "./ArrowIcon";
export default function ScheduleForm(){const[status,setStatus]=useState<"idle"|"loading"|"success"|"error">("idle");const[links,setLinks]=useState<{whatsapp?:string;mailto?:string}>({});async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus("loading");const data=Object.fromEntries(new FormData(e.currentTarget).entries());const response=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...data,type:"Solicitação de reunião",interest:"Reunião de diagnóstico",message:`Data preferida: ${data.date}\nPeríodo: ${data.period}\nAssunto: ${data.subject}`})});if(!response.ok){setStatus("error");return}setLinks(await response.json());setStatus("success")}return <form className="lead-form" onSubmit={submit}><div className="form-grid"><label>Nome<input name="name" required autoComplete="name"/></label><label>Empresa<input name="company" autoComplete="organization"/></label><label>E-mail<input name="email" type="email" required autoComplete="email"/></label><label>WhatsApp<input name="phone" required autoComplete="tel"/></label><label>Data preferida<input name="date" type="date" required/></label><label>Melhor período<select name="period" required defaultValue=""><option value="" disabled>Selecione</option><option>Manhã</option><option>Tarde</option><option>Noite</option></select></label></div><label>Assunto da conversa<textarea name="subject" rows={4} required placeholder="Descreva brevemente o que deseja conversar."/></label><label className="consent"><input type="checkbox" required/><span>Autorizo o contato para confirmação da reunião.</span></label><button className="button" disabled={status==="loading"}>{status==="loading"?"Preparando…":"Solicitar horário"}<ArrowIcon diagonal/></button>{status==="success"&&<div className="form-result"><strong>Horário solicitado.</strong><p>A reunião será confirmada pela equipe. Continue pelo canal de sua preferência.</p><div>{links.whatsapp&&<a href={links.whatsapp} target="_blank" rel="noreferrer">WhatsApp</a>}{links.mailto&&<a href={links.mailto}>E-mail</a>}</div></div>}{status==="error"&&<p className="form-error">Não foi possível concluir. Tente novamente.</p>}</form>}

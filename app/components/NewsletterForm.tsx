"use client";
import { FormEvent,useState } from "react";
import ArrowIcon from "./ArrowIcon";
export default function NewsletterForm(){const[status,setStatus]=useState<"idle"|"loading"|"success"|"error">("idle");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus("loading");const email=new FormData(e.currentTarget).get("email");const response=await fetch("/api/newsletter",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});setStatus(response.ok?"success":"error")}return <form className="newsletter-form" onSubmit={submit}><label htmlFor="newsletter-email">Novidades úteis, sem excesso.</label><div><input id="newsletter-email" name="email" type="email" required placeholder="Seu melhor e-mail" aria-label="E-mail para receber novidades"/><button aria-label="Cadastrar e-mail" disabled={status==="loading"}><ArrowIcon/></button></div><small>{status==="success"?"Cadastro recebido. Confirme pelo e-mail quando a integração estiver ativa.":status==="error"?"Não foi possível cadastrar agora.":"Tecnologia, sistemas e decisões digitais."}</small></form>}

import { email as companyEmail } from "../../data";
export async function POST(request:Request){const{email}=await request.json() as{email?:string};if(!email||!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email))return Response.json({error:"E-mail inválido."},{status:400});let delivered=false;if(process.env.RESEND_API_KEY){const response=await fetch("https://api.resend.com/emails",{method:"POST",headers:{Authorization:`Bearer ${process.env.RESEND_API_KEY}`,"Content-Type":"application/json"},body:JSON.stringify({from:process.env.CONTACT_FROM_EMAIL||"LESystems Site <onboarding@resend.dev>",to:[process.env.CONTACT_TO_EMAIL||companyEmail],subject:"[LESystems] Novo cadastro na newsletter",text:`Novo cadastro na newsletter: ${email}`})});delivered=response.ok}return Response.json({received:true,delivered})}

import type { Metadata } from "next";
import Link from "next/link";
import SiteHeader from "../components/SiteHeader";
import SiteFooter from "../components/SiteFooter";
import { services } from "../data";
export const metadata: Metadata = { title: "Soluções", description: "Conheça as soluções digitais da LESystems." };
export default function ServicesPage() { return <main className="site-shell"><SiteHeader /><section className="inner-hero"><div className="mesh-bg" /><div className="container inner-centered"><p className="eyebrow"><span /> Central de soluções</p><h1>Tecnologia escolhida pelo <em>problema que resolve.</em></h1><p>Explore cada frente, compare possibilidades e avance para uma avaliação personalizada.</p></div></section><section className="section"><div className="container services-overview">{services.map((service) => <article key={service.slug}><div><span>{service.number}</span><div><h2>{service.title}</h2><p>{service.short}</p></div></div><ul>{service.includes.slice(0, 3).map((item) => <li key={item}>✓ {item}</li>)}</ul><Link href={`/servicos/${service.slug}`} className="button">Conhecer solução <span>→</span></Link></article>)}</div></section><section className="section page-cta"><div className="container contact-card light-card"><div><p className="eyebrow"><span /> Ainda em dúvida?</p><h2>Conte-nos o desafio.<br />Você não precisa conhecer <em>a solução.</em></h2><p>A equipe ajuda a identificar o caminho mais adequado para sua empresa.</p><Link href="/contato" className="button">Receber orientação <span>↗</span></Link></div></div></section><SiteFooter /></main>; }

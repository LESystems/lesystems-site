import { cookies } from "next/headers";import { accessToken,verifyAccessCode,type AccessArea } from "../../access";
export async function POST(request:Request){const{area,code}=await request.json() as{area?:AccessArea;code?:string};if(area!=="client"&&area!=="admin")return Response.json({error:"Área inválida."},{status:400});if(!verifyAccessCode(area,code))return Response.json({error:"Código de acesso inválido."},{status:401});const jar=await cookies();jar.set(`lesystems_${area}`,accessToken(area),{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"strict",path:"/",maxAge:60*60*8});return Response.json({ok:true})}

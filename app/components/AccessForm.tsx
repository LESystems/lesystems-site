"use client";import{FormEvent,useState}from"react";import ArrowIcon from"./ArrowIcon";
export default function AccessForm({area}:{area:"client"|"admin"}){const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const code=new FormData(e.currentTarget).get("code");const response=await fetch("/api/access",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({area,code})});if(response.ok){location.reload();return}const data=await response.json();setError(data.error||"Não foi possível acessar.");setLoading(false)}return <form className="access-form" onSubmit={submit}><label>Código de acesso<input name="code" type="password" required autoComplete="current-password" placeholder="Digite seu código"/></label><button className="button" disabled={loading}>{loading?"Verificando…":"Acessar área segura"}<ArrowIcon/></button>{error&&<p className="form-error">{error}</p>}</form>}
